# ACE FlowSmith - organisation knowledge base (the "map" the agent learns from).
# Each [pattern] block is a reusable integration pattern the agent can recommend
# and generate. Plain key=value so it stays dependency-free and editable.

[pattern]
id = ptp_file
title = PTP File-to-File
integrationType = PTP
connectivity = FILE
templateDir = Existing_Templates/subsys_ptp_appnm_funcnm_file
appProject = SUBSYS_PTP_APPNM_FUNCNM_FIL
requiredTokens = SUBSYS,APPNM,FUNCNM
optionalTokens =
keywords = point to point,ptp,file to file,file transfer,move file,directory to directory,passthrough,batch file,convert,transform,xml to json,json to xml,mapping,field mapping,data transformation,format conversion
description = Point-to-point batch flow: reads files, resets the content descriptor and routes file-to-file via the ED6 batch fileIn/fileOut connector subflows. Supports field mappings for XML to JSON transformations.

[pattern]
id = pub_file
title = PUB Publisher (File to MQ)
integrationType = PUB
connectivity = FILE
templateDir = Existing_Templates/subsys_pub_appnm_funcnm_file
appProject = SUBSYS_PUB_APPNM_FUNCNM_FIL
requiredTokens = SUBSYS,APPNM,FUNCNM
optionalTokens = NDMNM
keywords = publish,publisher,pub,file to queue,file to mq,emit event,broadcast,produce message,downstream
description = Publisher batch flow: reads files and publishes onto an ESB MQ queue via the ED6 pub_file_connector subflow. Generates embedded MQ queue definitions.

[pattern]
id = sub_file_pubonline
title = SUB Subscriber (Online publishing)
integrationType = SUB
connectivity = FILE
templateDir = Existing_Templates/subsys_sub_appnm_funcnm_file_pubonline
appProject = SUBSYS_SUB_APPNM_FUNCNM_FIL
requiredTokens = SUBSYS,APPNM,FUNCNM
optionalTokens = NDMNM
keywords = subscribe,subscriber,sub,consume,queue to file,mq to file,online,real time consumer,receive message
description = Subscriber batch flow tuned for ONLINE publishing: consumes from MQ, detects last message in group (isLastMsgGroup) and writes files.

[pattern]
id = sub_file_pubbatch
title = SUB Subscriber (Batch publishing)
integrationType = SUB
connectivity = FILE
templateDir = Existing_Templates/subsys_sub_appnm_funcnm_file_pubbatch
appProject = SUBSYS_SUB_APPNM_FUNCNM_FIL
requiredTokens = SUBSYS,APPNM,FUNCNM
optionalTokens = NDMNM
keywords = subscribe batch,subscriber batch,sub batch,consume batch,grouped messages,end of batch,trigger on last,queue to file batch
description = Subscriber batch flow tuned for BATCH publishing: consumes from MQ and uses an IsTrigger compute (Adapter_IsTrigger) to fire only on the last message of a batch group.
